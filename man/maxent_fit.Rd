% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{maxent_fit}
\alias{maxent_fit}
\title{Fit a scalar CPU maximum-entropy model}
\usage{
maxent_fit(
  x,
  presence,
  background = NULL,
  presence_weights = NULL,
  background_weights = NULL,
  features = c("linear", "quadratic"),
  thresholds = NULL,
  knots = NULL,
  regularization = list(lambda1 = 0, lambda2 = 1),
  control = list(max_iter = 2000L, tol = 1e-08, step = 1)
)
}
\arguments{
\item{x}{Numeric presence predictors when \code{background} is supplied, or a
combined table when \code{background} is \code{NULL}.}

\item{presence}{Logical presence indicator aligned with \code{x}, or a numeric
presence predictor table when \code{background} is supplied.}

\item{background}{Optional numeric background predictor table.}

\item{presence_weights}{Optional positive presence weights.}

\item{background_weights}{Optional positive background weights.}

\item{features}{Feature classes: \code{"linear"}, \code{"quadratic"}, \code{"product"},
\code{"threshold"}, \code{"hinge"}, or categorical-only models.}

\item{thresholds}{Optional named list of numeric threshold values by predictor.}

\item{knots}{Optional named list of numeric hinge knots by predictor.}

\item{regularization}{A list with non-negative \code{lambda1} and \code{lambda2}, and
optional feature-specific non-negative \code{penalty_l1} and \code{penalty_l2} vectors.}

\item{control}{A list with \code{max_iter}, \code{tol}, \code{step}, optional \code{device},
\code{dtype}, \code{engine} (\code{"analytic"} or \code{"torch"}), and \code{accelerated}.}
}
\value{
An object of class \code{maxent_fit}.
}
\description{
Fit a scalar CPU maximum-entropy model
}
