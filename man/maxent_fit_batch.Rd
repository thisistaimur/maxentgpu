% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{maxent_fit_batch}
\alias{maxent_fit_batch}
\title{Fit independent species models from a validated batch specification}
\usage{
maxent_fit_batch(
  x,
  species = NULL,
  background = NULL,
  presence_weights = NULL,
  ...,
  control = list()
)
}
\arguments{
\item{x}{A named list of species records. Each record must contain \code{presence}
and \code{background} tables, unless \code{background} is supplied separately, in which
case each element is a presence table.}

\item{species}{Optional character species IDs. Required when \code{x} is unnamed.}

\item{background}{Optional shared background table.}

\item{presence_weights}{Optional list of positive per-row weight vectors,
one per species. Names, when supplied, must match species IDs.}

\item{...}{Arguments forwarded to \code{\link[=maxent_fit]{maxent_fit()}} except \code{control}.}

\item{control}{Solver controls forwarded to \code{\link[=maxent_fit]{maxent_fit()}}. Set
\code{batch_solver = "torch"} for the experimental shared-design batched solver.}
}
\value{
An object of class \code{maxent_batch_model}.
}
\description{
This batch API preserves independent-model semantics and stable species ordering.
Set \code{control$batch_solver = "torch"} to use the experimental shared-design
batched L2 solver; unsupported inputs use the scalar path unless explicitly
requested, in which case a clear error is returned.
}
