# Save a fitted model

Save a fitted model

## Usage

``` r
save_maxent_model(object, file)
```

## Arguments

- object:

  A fitted `maxent_fit` object.

- file:

  Destination `.rds` path.

## Value

`file`, invisibly.
