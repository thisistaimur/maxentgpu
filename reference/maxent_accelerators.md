# List available MaxEnt-GPU compute backends

List available MaxEnt-GPU compute backends

## Usage

``` r
maxent_accelerators()
```

## Value

A character vector. CPU is returned only when Torch can execute the CPU
smoke operation.
