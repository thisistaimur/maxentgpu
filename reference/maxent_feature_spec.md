# Return a fitted model's immutable feature specification

Return a fitted model's immutable feature specification

## Usage

``` r
maxent_feature_spec(object)
```

## Arguments

- object:

  A fitted `maxent_fit` object.

## Value

A `maxent_feature_spec` object.
