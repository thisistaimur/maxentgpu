# Load a fitted model

Load a fitted model

## Usage

``` r
read_maxent_model(file)
```

## Arguments

- file:

  Path created by
  [`save_maxent_model()`](https://thisistaimur.github.io/maxentgpu/reference/save_maxent_model.md).

## Value

A fitted `maxent_fit` object.
